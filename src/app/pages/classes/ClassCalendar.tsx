// DEPRECATED — superseded by the Unified Calendar (src/app/pages/calendar/UnifiedCalendar.tsx).
// This page's route and nav entry have been removed. It is kept on disk only
// because this environment could not delete the file directly — safe to
// delete src/app/pages/classes/ClassCalendar.tsx manually.
export default function ClassCalendar() {
  return null;
}
